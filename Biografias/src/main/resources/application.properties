spring.application.name=Biografias

# → URL: apunta a tu contenedor docker
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=BiografiasDB;encrypt=false;trustServerCertificate=true

# → Credenciales (por defecto en el contenedor: usuario "sa")
spring.datasource.username=sa
spring.datasource.password=Abc12345!

# → Fuerza explícitamente el driver si aún no lo detecta
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# → Opciones JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
